"""Introsort (introspective sort): hybrid quicksort + heapsort + insertion.\n\nComplexity: O(n log n) worst-case. Original implementation.\nSwitches to heapsort after depth limit, insertion for small partitions.\n"""\nfrom __future__ import annotations\n\nfrom typing import MutableSequence, TypeVar\nimport math\n\nT = TypeVar(\"T\")\n\n\ndef _insertion(a: MutableSequence[T], lo: int, hi: int) -> None:\n    for i in range(lo + 1, hi + 1):\n        key = a[i]\n        j = i - 1\n        while j >= lo and a[j] > key:\n            a[j + 1] = a[j]\n            j -= 1\n        a[j + 1] = key\n\n\ndef _heapify(a: MutableSequence[T], n: int, i: int, offset: int) -> None:\n    while True:\n        largest = i\n        l = 2 * i + 1\n        r = 2 * i + 2\n        if l < n and a[offset + l] > a[offset + largest]:\n            largest = l\n        if r < n and a[offset + r] > a[offset + largest]:\n            largest = r\n        if largest == i:\n            break\n        a[offset + i], a[offset + largest] = a[offset + largest], a[offset + i]\n        i = largest\n\n\ndef _heapsort(a: MutableSequence[T], lo: int, hi: int) -> None:\n    n = hi - lo + 1\n    for i in range(n // 2 - 1, -1, -1):\n        _heapify(a, n, i, lo)\n    for i in range(n - 1, 0, -1):\n        a[lo], a[lo + i] = a[lo + i], a[lo]\n        _heapify(a, i, 0, lo)\n\n\ndef _median3(a: MutableSequence[T], lo: int, mid: int, hi: int) -> T:\n    if a[lo] > a[mid]:\n        a[lo], a[mid] = a[mid], a[lo]\n    if a[lo] > a[hi]:\n        a[lo], a[hi] = a[hi], a[lo]\n    if a[mid] > a[hi]:\n        a[mid], a[hi] = a[hi], a[mid]\n    return a[mid]\n\n\ndef _partition(a: MutableSequence[T], lo: int, hi: int) -> int:\n    mid = (lo + hi) // 2\n    pivot = _median3(a, lo, mid, hi)\n    a[mid], a[hi - 1] = a[hi - 1], a[mid]\n    i = lo\n    j = hi - 1\n    while True:\n        i += 1\n        while a[i] < pivot:\n            i += 1\n        j -= 1\n        while a[j] > pivot:\n            j -= 1\n        if i >= j:\n            break\n        a[i], a[j] = a[j], a[i]\n    a[i], a[hi - 1] = a[hi - 1], a[i]\n    return i\n\n\ndef _introsort(a: MutableSequence[T], lo: int, hi: int, depth: int) -> None:\n    while hi - lo > 16:\n        if depth == 0:\n            _heapsort(a, lo, hi)\n            return\n        depth -= 1\n        p = _partition(a, lo, hi)\n        _introsort(a, p + 1, hi, depth)\n        hi = p - 1\n    _insertion(a, lo, hi)\n\n\ndef introsort(a: MutableSequence[T]) -> None:\n    \"\"\"Sort a in-place ascending using introsort.\"\"\"\n    n = len(a)\n    if n < 2:\n        return\n    maxdepth = 2 * int(math.log2(n)) + 1\n    _introsort(a, 0, n - 1, maxdepth)\n\n\nif __name__ == \"__main__\":\n    a = [9, 8, 3, 7, 5, 6, 4, 1]\n    introsort(a)\n    assert a == sorted([9, 8, 3, 7, 5, 6, 4, 1])\n    b: list[int] = []\n    introsort(b)\n    assert b == []\n    c = [42]\n    introsort(c)\n    assert c == [42]\n    d = [5] * 12\n    introsort(d)\n    assert d == [5] * 12\n    e = list(range(50, 0, -1))\n    introsort(e)\n    assert e == list(range(1, 51))\n    f = [3, 1, 4, 1, 5, 9, 2, 6]\n    introsort(f)\n    assert f == [1, 1, 2, 3, 4, 5, 6, 9]\n    print(\"introsort self-tests passed\")\n